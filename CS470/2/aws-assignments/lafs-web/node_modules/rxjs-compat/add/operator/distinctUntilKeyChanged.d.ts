import { distinctUntilKeyChanged } from 'rxjs-compat/operator/distinctUntilKeyChanged';
declare module 'rxjs/internal/Observable' {
    interface Observable<T> {
        distinctUntilKeyChanged: typeof distinctUntilKeyChanged;
    }
}
